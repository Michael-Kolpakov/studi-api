apiVersion: v1
kind: Service
metadata:
  name: teachio-api
  namespace: teachio
spec:
  selector:
    app: teachio-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
